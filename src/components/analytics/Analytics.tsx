import React from 'react';
import { Box, Typography } from '@mui/material';

export const Analytics: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        Аналітика
      </Typography>
      {/* Тут буде контент аналітики */}
    </Box>
  );
};